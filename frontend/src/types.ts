export interface Book {
    id: number;
    title: string;
    author: string;
    description: string;
}

export interface NewBook {
    title: string;
    author: string;
    description: string;
}
